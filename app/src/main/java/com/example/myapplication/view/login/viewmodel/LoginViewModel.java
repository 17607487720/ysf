package com.example.myapplication.view.login.viewmodel;

import com.example.myapplication.http.LoginService;
import com.example.myapplication.model.BaseViewModel;

public class LoginViewModel extends BaseViewModel {
    private LoginService service;

    public void init(){
//        RetrofitUtils.getInstance().getUserRetrofit(App.getInstance().getApplicationContext()).create(LoginService.class).version()...
    }
}
